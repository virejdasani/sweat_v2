const WIP = () => {
  return (
    <h1 className="text-center mt-5">
      Working on getting coursework and exam data dynamically to show on the
      calendar and show the graph under that data.
    </h1>
  );
};

export default WIP;
